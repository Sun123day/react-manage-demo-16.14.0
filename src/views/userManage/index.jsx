import styles from './index.module.scss'

import React from 'react'

const UserManage = () => {
  return (
    <div>这个是用户管理
      <div>
        接下来要在这里完成的内容有：
        1，表格的封装（含分页控制）
        2，弹窗的封装，使用new this.$pageModal(FormModal, {}, (result) => {})的这种方式
        3，看看能不能把删除的封装了，不然太难受了，每次警告框架需要配置太多内容
    
      </div>
    </div>
  )
}
export default UserManage